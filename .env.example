# Green API Configuration
GREEN_API_INSTANCE_ID="YOUR_INSTANCE_ID"
GREEN_API_TOKEN="YOU_API_TOKEN"

# Google Gemini AI Configuration
GOOGLE_API_KEY="YOUR_API_KEY"
GEMINI_LLM_MODEL="models/gemini-flash-latest"

# Notification Configuration
SUMMARY_RECIPIENT_PHONE="+YOUR PHONE NUMBER"
SUMMARY_SCHEDULE_HOUR="20"
SUMMARY_SCHEDULE_TIMEZONE="Asia/Jerusalem"

# Application Configuration
LOG_LEVEL="INFO"
MINIMUM_MESSAGES_FOR_SUMMARY="1"
MAX_MESSAGES_PER_SUMMARY="1000"
SUMMARY_KEYWORDS="sikum,סיכום,summary,summarize"

# Security Configuration
# IMPORTANT: Set these for production to secure your WhatsApp data!

# Encryption key for database encryption (protects message content and summaries)
# Generate a secure key by running:
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# Leave empty to disable encryption (NOT RECOMMENDED for production)
ENCRYPTION_KEY=""

# Webhook secret for API authentication (prevents unauthorized webhook access)
# Generate a secure random string (e.g., a UUID or random password)
# Leave empty to disable webhook authentication (NOT RECOMMENDED for production)
WEBHOOK_SECRET=""